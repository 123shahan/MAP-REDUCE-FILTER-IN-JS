// Map method is used for creating a new array with the help of the existing array after apllying fucntion on the existing array.

const number1 = [1,2,3,4];
const number2 = number1.map(Element=>Element * 2);
console.log(number2);